/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatars.githubusercontent.com",
      "pirooo.s3.amazonaws.com",
      "www.loom.com",
    ],
  },
};

export default nextConfig;
